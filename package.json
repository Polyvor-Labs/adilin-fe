{
  "name": "justika-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node dev-server.mjs",
    "check": "node --check static/app.js && node --check dev-server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
